generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum EventStatus {
  DRAFT
  PENDING
  PUBLISHED
  REJECTED
}

enum EventRevisionStatus {
  DRAFT
  PENDING
  REJECTED
}

enum GeolocationPrecision {
  EXACT
  APPROXIMATE
  UNRESOLVED
}

enum UserRole {
  EDITOR
  MODERATOR
  ADMIN
}

model Event {
  id               String      @id @default(uuid()) @db.Uuid
  title            String
  content          String?
  image            String?
  createdByUserId  String?     @db.Uuid
  categoryId       String?
  audienceId       String?
  organizerName    String?
  organizerUrl     String?
  contactEmail     String?
  contactPhone     String?
  ticketUrl        String?
  pricingInfo      String?
  websiteUrl       String?
  socialLinks      Json?
  status           EventStatus @default(DRAFT)
  featured         Boolean     @default(false)
  publishedAt      DateTime?
  publicationEndAt DateTime
  rejectionReason  String?
  createdAt        DateTime    @default(now())
  updatedAt        DateTime    @updatedAt

  category         Category?   @relation(fields: [categoryId], references: [id], onDelete: Restrict, onUpdate: Cascade)
  audience         Audience?   @relation(fields: [audienceId], references: [id], onDelete: Restrict, onUpdate: Cascade)
  createdByUser    User?       @relation("UserCreatedEvents", fields: [createdByUserId], references: [id], onDelete: SetNull, onUpdate: Cascade)
  pendingRevision  EventRevision?
  occurrences      EventOccurrence[]

  @@index([status])
  @@index([featured])
  @@index([categoryId])
  @@index([audienceId])
  @@index([createdByUserId])
}

model EventOccurrence {
  id                   String               @id @default(uuid()) @db.Uuid
  eventId              String               @db.Uuid
  venueName            String?
  address              String?
  postalCode           String?
  city                 String?
  latitude             Float?
  longitude            Float?
  geolocationPrecision GeolocationPrecision @default(UNRESOLVED)
  eventStartAt         DateTime?
  eventEndAt           DateTime?
  allDay               Boolean?
  createdAt            DateTime             @default(now())
  updatedAt            DateTime             @updatedAt

  event                Event                @relation(fields: [eventId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@index([eventId])
  @@index([eventStartAt])
}

model EventRevision {
  id              String              @id @default(uuid()) @db.Uuid
  eventId         String              @unique @db.Uuid
  title           String
  content         String?
  image           String?
  createdByUserId String?             @db.Uuid
  categoryId      String?
  audienceId      String?
  organizerName   String?
  organizerUrl    String?
  contactEmail    String?
  contactPhone    String?
  ticketUrl       String?
  pricingInfo     String?
  websiteUrl      String?
  socialLinks     Json?
  status          EventRevisionStatus @default(PENDING)
  featured        Boolean             @default(false)
  rejectionReason String?
  createdAt       DateTime            @default(now())
  updatedAt       DateTime            @updatedAt

  event           Event               @relation(fields: [eventId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  category        Category?           @relation(fields: [categoryId], references: [id], onDelete: Restrict, onUpdate: Cascade)
  audience        Audience?           @relation(fields: [audienceId], references: [id], onDelete: Restrict, onUpdate: Cascade)
  createdByUser   User?               @relation("UserCreatedRevisions", fields: [createdByUserId], references: [id], onDelete: SetNull, onUpdate: Cascade)
  occurrences     EventRevisionOccurrence[]

  @@index([status])
  @@index([categoryId])
  @@index([audienceId])
  @@index([createdByUserId])
}

model EventRevisionOccurrence {
  id                   String               @id @default(uuid()) @db.Uuid
  eventRevisionId      String               @db.Uuid
  venueName            String?
  address              String?
  postalCode           String?
  city                 String?
  latitude             Float?
  longitude            Float?
  geolocationPrecision GeolocationPrecision @default(UNRESOLVED)
  eventStartAt         DateTime?
  eventEndAt           DateTime?
  allDay               Boolean?
  createdAt            DateTime             @default(now())
  updatedAt            DateTime             @updatedAt

  eventRevision        EventRevision        @relation(fields: [eventRevisionId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@index([eventRevisionId])
  @@index([eventStartAt])
}

model Category {
  id        String   @id
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  events    Event[]
  revisions EventRevision[]
}

model Audience {
  id        String   @id
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  events    Event[]
  revisions EventRevision[]
}

model User {
  id           String   @id @default(uuid()) @db.Uuid
  name         String
  email        String   @unique
  role         UserRole
  passwordHash String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  createdEvents        Event[]              @relation("UserCreatedEvents")
  createdRevisions     EventRevision[]      @relation("UserCreatedRevisions")
  passwordResetTokens  PasswordResetToken[]
}

model PasswordResetToken {
  id        String   @id @default(uuid()) @db.Uuid
  tokenHash String   @unique
  userId    String   @unique @db.Uuid
  expiresAt DateTime
  createdAt DateTime @default(now())

  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@index([expiresAt])
}

model SiteSetting {
  id               String   @id @default("default")
  contactEmail     String   @default("contact@rene-website.test")
  contactPhone     String   @default("0102030405")
  homepageIntro    String   @default("Plateforme culturelle de Descartes.")
  homepageSubtitle String   @default("")
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}
